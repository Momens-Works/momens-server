/**
 * project capability의 Spring Modulith 경계.
 *
 * <p>공개 계약은 하위 도메인별 named interface에 두며 이 패키지는 namespace와 모듈 선언만 소유합니다.
 */
@org.springframework.modulith.ApplicationModule
package works.momens.server.project;
