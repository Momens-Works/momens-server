rootProject.name = 'momens-server'

// app: 실행·조립 모듈. common: 공유 영속성 베이스. user/auth/workspace/project: 기능/도메인 모듈.
// mobile: 모바일 진입 API가 도메인 모듈 public API를 조합하는 얇은 orchestration 모듈.
// 도메인 모듈 목록은 기능이 추가되며 점진적으로 늘립니다.
include 'app'
include 'common'
include 'user'
include 'auth'
include 'workspace'
include 'project'
include 'mobile'
