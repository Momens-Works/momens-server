import org.springframework.boot.gradle.plugin.SpringBootPlugin

plugins {
    id 'java-library'
}

dependencies {
    // Boot BOM으로 버전을 관리합니다(boot 플러그인은 app만 적용).
    implementation platform(SpringBootPlugin.BOM_COORDINATES)
    annotationProcessor platform(SpringBootPlugin.BOM_COORDINATES)
    testImplementation platform(SpringBootPlugin.BOM_COORDINATES)

    // 공유 영속성 베이스(BaseEntity, JPA Auditing)와 공유 에러 계약.
    implementation project(':common')
    // 접근 가능한 project 목록 조회가 workspace public API(WorkspaceAccess.listWorkspaceIds)를
    // 사용합니다(module-map 의존 방향). workspaces 테이블 마이그레이션도 이 의존으로 테스트
    // 클래스패스에 올라갑니다(projects.workspace_id FK).
    implementation project(':workspace')

    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'

    compileOnly 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'

    // common testFixtures의 Testcontainers 통합테스트 베이스를 재사용합니다.
    testImplementation testFixtures(project(':common'))
    // projects.owner_id는 users(id)를 FK로 참조합니다. 슬라이스 통합테스트가 Flyway로 users
    // 테이블을 만들 수 있도록 user 모듈 마이그레이션을 테스트 클래스패스에 올립니다(테스트 스코프,
    // User 엔티티는 project 패키지 밖이라 매핑되지 않습니다).
    testImplementation project(':user')
}
